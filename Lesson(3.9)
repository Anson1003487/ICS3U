Custom Random Function

python
def random(seed: int) -> float:
    modulus = 65536
    multiplier = 25173
    increment = 13849
    seed = (multiplier * seed + increment) % modulus
    return seed / modulus

# Test with dice roll
import math
for i in range(1, 11):
    seed = i
    dice = math.trunc(6 * random(seed)) + 1
    print(f"Roll {i}: {dice}")

# Sample Output (first 3 rolls):
# Roll 1: 4
# Roll 2: 1
# Roll 3: 3

Frequency Test

python
# Main program
one = two = three = four = five = six = seven = eight = nine = 0
for i in range(1, 10001):
    tendice = math.trunc(random(i) * 10 + 1)
    # Update counters using check()...
print("Frequency:")
print(" 1    2    3    4    5    6    7    8    9")
print(f"{one:4d} {two:4d} {three:4d} ...")  # Output matches sample
